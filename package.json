{
  "name": "storyblok-translation-monorepo",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@8.15.7",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
